# Junk / Local Environment
.venv/
venv/
__pycache__/
*.pyc
.vscode/
.vs/
*.swp
*.log

# Build Outputs (Compiled stuff we don't want)
*.obj
*.lib
*.cpe
*.map
*.sym
obj/
obj_vr/
obj_dev/
obj_psx_dev/
build/include_asm_tmp/

# Executables / Binaries (Except the ones we need)
# If you have specific tool binaries, they should be tracked
# but general build artifacts should be ignored.
